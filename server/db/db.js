//TODO: depedencies
//TODO: signal flow for SQL vs No-SQL (TBD - after seed)

const DB = {
  getSongs: (id) => {
    //placeholder
    return new Promise();
  },
  create: (id, record) => {
    //placeholder
    return new Promise();
  },
  read: (id) => {
    //placeholder
    return new Promise();
  },
  update: (id, record) => {
    //placeholder
    return new Promise();
  },
  delete: (id) => {
    return new Promise();
  }
};

export default DB;
